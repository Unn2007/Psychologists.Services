import css from './Detalies.module.css'
import { useParams } from "react-router-dom";

export const Detalies = ()=> {
    // const { id } = useParams();

    return <h2>Details for Psychologist ID</h2>;
   
}